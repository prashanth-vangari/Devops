git --version
git help
git help pull  // gives the information regarding the pull command 
git config --global user.name "Prashanth Vangari"
git config --global user.email "vangariprashanth001@gmail.com"
git config --list
pwd 
git init
git commit // Used to move the working directory to staging area 
git status
git add first_file.txt// It git starts to tracks the files
git add .    // 'period' specifies to add all the files
git commit -m "my first commit"   // what is the purpose of the commit what is the changes you are adding to the commit
git log --author="Prashanth Vangari"
git commit -am "This adds the file and also commits the file"
Observation: 
Git diff works only when the differences are between uncommitted file and the committed file
